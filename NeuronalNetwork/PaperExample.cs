using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace test
{
   class PaperExample : NeuralTask
   {
      /*
      double[] input = { 0.35, 0.9 };
      double[] expected = { 0.5 };
      double[] neuronWeights = { 0, 0.1, 0.8, 0, 0.4, 0.6, 0, 0.3, 0.9 };
      int[] topology = { 2, 1 };

      new void Init()
      {
         n.Init(input, topology, neuronWeights);
      }

      public new double Iterate()
      {
         
         n.Forward();
         double err = n.BackPropagate(input, expected);
         n.UpdateWeights();

         return err;
          
         return 0;
      }
      */
   }
}
