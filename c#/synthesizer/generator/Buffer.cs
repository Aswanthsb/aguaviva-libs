using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace midiplayer.generator
{
   /*
   class Buffer : SoundGenerator
   {
      byte[] m_rawsamples;

      public Buffer(string name, uint sampleRate)
         : base(name, sampleRate)
      {

         m_input = new NullGenerator("null", 44100);
         m_inputs["input"] = new variable.SetObject(SetInput);


         applicationDevice = null;
         secondaryBuffer = null;
      }

   }
    */
}
